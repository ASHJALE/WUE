# Copy this file to .env and replace each placeholder with local PostgreSQL values.
# URL-encode special characters in the username or password (for example, @ as %40).
DATABASE_URL=postgresql+psycopg://YOUR_USER:YOUR_PASSWORD@localhost:5432/wue_db

# Replace this development placeholder with a long, random secret in .env.
JWT_SECRET_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# Local five-class furniture classifier. The fallback is deliberately off by default.
FURNITURE_CLASSIFIER_ENABLED=true
FURNITURE_MODEL_BACKEND=onnx
FURNITURE_MODEL_PATH=models/furniture_classifier.onnx
FURNITURE_MODEL_VERSION=uninstalled
FURNITURE_MIN_CONFIDENCE=0.50
FURNITURE_MODEL_OUTPUT_IS_LOGITS=true
FURNITURE_DEVELOPMENT_FALLBACK_ENABLED=false
